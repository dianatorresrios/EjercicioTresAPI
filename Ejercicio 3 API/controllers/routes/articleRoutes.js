const express = require('express');
const router = express.Router();
const articleController = require('../controllers/articleController');

//ruta router.post('/articles', articleController.createArticle);

module.exports = router;
